{
  "name": "skillet",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm -r check",
    "check:fix": "pnpm -r check:fix",
    "dev:ui": "pnpm --dir packages/skillet dev:ui",
    "dev:eval-api": "pnpm --dir packages/skillet dev:eval-api",
    "release:tag": "node ./scripts/release-tag.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
